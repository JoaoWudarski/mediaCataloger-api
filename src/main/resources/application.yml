spring:
  application:
    name: media-cataloger-api
  profiles:
    active: ${ENVIRONMENT}

logging:
  level:
    org:
      springframework:
        web:
          client:
            RestTemplate: TRACE


token-config:
  issuer: "MEDIA-CATALOG"
  expirationTimeSeconds: 10800
  secretKey: "Dtpb2ley7RmAN8GDKcWNpnox31KZvZiZ"
